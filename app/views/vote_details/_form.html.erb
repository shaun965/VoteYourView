<%= form_for(@vote_detail) do |f| %>
  <% if @vote_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_detail.errors.count, "error") %> prohibited this vote_detail from being saved:</h2>

      <ul>
      <% @vote_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
